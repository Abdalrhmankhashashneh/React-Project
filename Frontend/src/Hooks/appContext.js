// create your context here (e.g. const AppContext = createContext();)
import { createContext } from "react"; //import createContext

//export the context here
export const AppContext = createContext(); 